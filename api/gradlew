#!/usr/bin/env bash
DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
if command -v java >/dev/null 2>&1; then
  export JAVA_HOME="${JAVA_HOME:-}"
fi
if command -v gradle >/dev/null 2>&1; then
  exec gradle -p "$DIR" "$@"
else
  echo "Gradle is required but was not found." >&2
  exit 1
fi
